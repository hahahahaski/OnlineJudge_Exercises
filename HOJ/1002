#include <iostream>

using namespace std;

int main()
{
    long long A = 0, B = 0, C = 0;
    while (cin >> A >> B >> C)
    {
        long long sum = A + B + C;
        cout << sum << endl;
    }
    return 0;
}
